'use strict';

var expect = require('chai').expect;

describe('subliminal-config', function() {
    it('Should have a list of all the messages', function() {
        
    })

    it('Should store the correct duration and interval', function() {
        
    })

    it('Should allow a random message in the list to be selected', function() {

    })

    it('Should Preserve the order of the message that was passed', function() {

    })


})